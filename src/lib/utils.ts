// Utilitaires partages par les composants UI.
// Aucun helper n'a ete necessaire pour le refactor de l'etape 14 (les classes conditionnelles
// des composants UI restent de simples ternaires locaux, pas de logique de fusion de classes
// dupliquee qui justifierait un cn()). Fichier volontairement minimal (YAGNI).
export {};
