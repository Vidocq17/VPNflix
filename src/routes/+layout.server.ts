import type { LayoutServerLoad } from './$types';

// TODO(etape 6): exposer l'utilisateur courant depuis event.locals (hooks.server.ts).
export const load: LayoutServerLoad = async () => {
	return {};
};
